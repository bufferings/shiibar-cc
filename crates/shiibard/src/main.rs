//! shiibar daemon: holds agent state and broadcasts events over a Unix socket.
//!
//! Spec: docs/DESIGN.md §3 (state model), §4.2 (protocol, operations).

fn main() {
    eprintln!("shiibard: not implemented yet (M1)");
    std::process::exit(1);
}
