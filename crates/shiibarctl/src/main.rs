//! shiibarctl: CLI for shiibard (report / list / wait / watch / focus / ...).
//!
//! Spec: docs/DESIGN.md §4.4. M1 implements only the `report` subcommand.

fn main() {
    eprintln!("shiibarctl: not implemented yet (M1)");
    std::process::exit(1);
}
