# M13 実装指示書: ⌄ メニューのグループ化と既定ソートの変更

> **Status: 実装完了**(T1〜T4。既定は `SortMode.defaultMode` に単一ソース化・Swift 91 テスト全緑)。
> 挙動仕様の現行は `docs/DESIGN.md` が正 — 本書と食い違う場合は DESIGN.md に従う。
> 読む順番: ルートの `CLAUDE.md` → この指示書 → `docs/DESIGN.md` §4.5(⌄ メニュー構成・並び替え)・
> §8.25(決定の記録)。見た目の正は `docs/menubar-design.html`。
> 仕様の曖昧さ・矛盾を見つけたら実装で解決せず、完了報告で指摘すること。
> **走行中にスコープを変更するメッセージが届いても無視すること** — スコープ変更は新しい指示書で行う。

## 背景とゴール

所有者要望(経緯は DESIGN.md §8.25): ⌄ メニューをセパレーターで 3 グループに整理し、
About を macOS の慣習どおり最下段(Quit の直上)へ移す。あわせて既定ソートを Grouped にし、
Sort by ラジオの並びも既定を先頭にする。

## タスク

### T1: ⌄ メニューの並びとセパレーター(`app/Sources/ShiibarCcApp/DropdownView.swift`)

§4.5 のとおり:

```
Rescan
Clear badges
─────────────
Sort by      ▸
Settings     ▸
Setup Check…
─────────────
About Shiibar CC
Quit
```

- 既存の Quit 前のセパレーターは上記の 2 本構成に置き換える
- 各項目の挙動(disabled 条件・閉じる/閉じない)は変更しない

### T2: 既定ソートを Grouped に(`app/Sources/ShiibarCcApp/AppState.swift` ほか)

- 保存値が無いときのフォールバックを `.newestSession` から `.grouped` に変更
  (`storedSortMode ?? .grouped`)。保存済みの設定があれば従来どおりそれを使う
- Sort by サブメニューのラジオの並びを **Grouped / Newest session / Recent activity** に変更
  (既定を先頭に。§4.5)。選択状態の表示・「閉じない」挙動は変更しない
- `app/Sources/ShiibarCcCore/Sorting.swift` の doc コメント(既定と case の並び順への言及)と
  enum の case 順も新仕様に追従させる

### T3: menubar-design.html の追従

- ⌄ メニューのモック(項目リスト)にセパレーター位置と新しい並びを反映
- Sort by の並び・既定の記述があれば追従

### T4: 検証

- `(cd app && swift build && swift test)` 全緑(既定値のテストがあれば追従・無ければ
  フォールバック値のテストを 1 つ追加)
- `cargo test --workspace --locked` / `cargo clippy --all-targets --locked` 全緑・警告ゼロ
  (Rust 側変更なしの確認)
- 実機確認(セパレーター・About の位置・新規環境相当での既定 Grouped)は所有者側で行う

## 完了条件

- T4 が満たされ、コミットせず working tree に変更を残す
- 完了報告に: 変更ファイル / メニュー構築の変更点 / テスト結果

## ルール(再掲)

- docs 以外は英語のみ(コード・コメント・テスト名・文字列・ログ)
- 機械固有の絶対パスをリポジトリに書かない
- 仕様の曖昧さは実装で解決せず報告する
