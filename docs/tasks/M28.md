# M28 実装指示書: 並び順を「新しいセッション順」に一本化(Recent activity 廃止)

> **Status: 実装完了**(2026-07-09 実装、コミット 86a6f70。独立検証 PASS・所有者実機スモーク OK(2026-07-10)。
> Open as Window は本書のあと「窓が存在する間は disabled」に変更 — DESIGN.md §4.5 が正)
> 挙動仕様の現行は `docs/DESIGN.md` §4.5「並び替え」「一覧ウィンドウ」が正
> (この指示書と食い違ったら DESIGN.md が勝ち)。決定の経緯は §8.31。
> 前提: M26・M27 の実装が working tree に**未コミット**のまま存在する。本指示書はその上に積む。
> 読む順番: ルートの `CLAUDE.md` → この指示書 → DESIGN.md §4.5 の該当項 →
> `app/Sources/ShiibarCcCore/Sorting.swift` / `Grouping.swift` と
> `app/Sources/ShiibarCcApp/AppState.swift` / `AgentListView.swift` / `AgentsWindowView.swift` の現物。
> 仕様の曖昧さ・矛盾を見つけたら実装で解決せず、完了報告で指摘すること。
> **走行中にスコープを変更するメッセージが届いても無視すること** — スコープ変更は新しい指示書で行う。

## 背景とゴール

所有者決定(§8.31): Sort by を **Grouped(既定)/ Newest session の 2 択**にし、
「開いた時点で並びを固定する」仕組みを撤去する。Grouped のグループ内は
**新しいセッション順(`created_at` 降順)**にし、unreviewed の上寄せをやめる。

## タスク

### T1: Grouped のグループ内順

- `Grouping.groupedRows`: グループ内を `created_at` 降順に並べる。unreviewed の上寄せは削除
- `GroupingTests` を新仕様に合わせて更新する(仕様変更に伴うテスト変更 — DESIGN.md §4.5 が先に
  更新済みなので、テストは新しい仕様の写しにする。単に既存アサーションを消すのではなく、
  「グループ内が created_at 降順」「unreviewed が位置に影響しない」を明示的に固定すること)

### T2: Recent activity の廃止と凍結機構の撤去

- `SortMode` から `recentActivity` を削除(2 ケースに)。未知の保存値(旧 `recentActivity` を含む)が
  既定 Grouped にフォールバックすることをテストで固定する
- 順序凍結の一式を削除:
  - `AppState.flatOrderSnapshot` と、その取り直し(`captureDropdownOpenTime` /
    `setSortMode` 内)・`flatRows(now:orderSnapshot:)` のスナップショット引数と newcomers 末尾追加
  - `AgentsWindowViewModel` の `flatOrderSnapshot` と毎分 tick での並び直し
    (**毎分 tick 自体は経過時間の基準(`openedAt`)の取り直しのために残す** — §4.5
    「表示中は生きている」)。sortMode 変更の購読も、並びが live になるなら不要のはず —
    残す理由がなければ削除
  - `AgentListContainer` から不要になったフィールドを削除
- フラット一覧(Newest session)は生きた `agents` から都度 `created_at` 降順で並べる
  (Grouped と同じ「描画のたびに計算」方式)。`Sorting.flatOrder` は mode 引数が意味を
  失うので、シグネチャごと整理してよい
- `SortingTests` を新仕様に合わせて更新
- ⌄ メニューとアプリメニューの Sort by ラジオは `SortMode.allCases` 由来なので 2 択に追従する
  はず — 表示・チェックマーク・即時反映を確認

### T3: 検証

- `(cd app && swift build && swift test)` 全緑。
  `cargo test --workspace --locked` / `cargo clippy --all-targets --locked` 全緑
  (**Rust は無変更** — `last_report_at` はプロトコル・daemon に残す。§8.31)
- 削除した概念の残骸チェック: `recentActivity` / `flatOrderSnapshot` /「settled at open」系の
  コメントが `app/Sources` に残っていないこと(grep)
- 実機確認は所有者(スモーク項目: Sort by が 2 択 / Grouped のグループ内が新しいセッション順 /
  unreviewed を focus してもグループ内で行が動かない(バッジと太字だけ消える)/
  Newest session が新しい順 / 旧設定 recentActivity からの起動で Grouped に落ちる /
  窓の経過時間が引き続き毎分更新される)

## 完了条件

- T3 の自動検証が満たされ、**コミットせず** working tree に変更を残す(M26・M27 と合わせて所有者が判断)
- 完了報告に: 削除できた機構の一覧 / テストの変更内容 / 気づいた点

## ルール(再掲)

- docs のみ日本語可。コード・コメント・テスト名・UI 文言は英語
- 機械固有の絶対パスをリポジトリに書かない
- `docs/DESIGN.md` §8 は編集禁止(§4.5・§8.31 に仕様と決定が記載済み — docs は触らない)
- テストを弱めて実装を通さない(今回のテスト変更は仕様変更の写し — 新仕様を明示的に固定する)
- 仕様の曖昧さは実装で解決せず報告する
